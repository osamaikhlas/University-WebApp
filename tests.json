{
  "$schema": "Test coverage manifest for the affiliated-college website and administration system. Update alongside any test work. Never remove or weaken an entry to make coverage look better than it is (CLAUDE.md rule 10) \u2014 change its status honestly instead.",
  "lastUpdated": "2026-09-16T02:10:00Z",
  "statusValues": [
    "not_started",
    "planned",
    "in_progress",
    "passing",
    "failing",
    "skipped"
  ],
  "foundation": {
    "description": "Phase 1 (project foundation) infrastructure tests \u2014 not tied to a specific circular requirement, unlike the per-feature entries below which stay not_started until their real (non-placeholder) behavior is built.",
    "items": [
      {
        "id": "fnd-env-validation",
        "feature": "Environment variable validation (src/lib/env.ts)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/env.test.ts \u2014 6 cases (valid input, defaults, missing/invalid DATABASE_URL, invalid NODE_ENV), plus GRIEVANCE_ENCRYPTION_KEY and NEXT_PUBLIC_SITE_URL format/default cases added in later phases. Run via `npm test`."
      },
      {
        "id": "fnd-route-structure-public",
        "feature": "Public route structure (20 consolidated Phase 4 sections)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/routes.test.ts \u2014 asserts link count, a page.tsx exists for every link, no duplicate hrefs. Phase 4 consolidated the original 29 one-per-circular-item routes into 20 broader sections (see progress.md's decisions log for the mapping)."
      },
      {
        "id": "fnd-route-structure-admin",
        "feature": "Admin route structure matches CLAUDE.md scope (37 sections, including College Profile/Departments/Seminars/Workshops/Fee Structures/Enrollment Statistics/Activities/Clubs/Infrastructure/Policies/Regulations/Affiliation/Contact/Location CMS modules added after the original 23)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/routes.test.ts \u2014 same checks as public route structure."
      },
      {
        "id": "fnd-page-placeholder",
        "feature": "Shared PagePlaceholder component",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/PagePlaceholder.test.tsx \u2014 renders title/description, always shows a placeholder marker (rule 14), renders circular reference when provided."
      },
      {
        "id": "fnd-public-site-e2e",
        "feature": "Public site loads end-to-end (home, all nav links, 404 handling)",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/public-site.spec.ts. Playwright's Chromium browser is now installed in this environment (`npx playwright install chromium`) and the full suite has been run for real via `npx playwright test`, not just listed."
      },
      {
        "id": "fnd-admin-site-e2e",
        "feature": "Admin site loads end-to-end (auth-gated dashboard, all nav links redirect correctly when unauthenticated)",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/admin.spec.ts \u2014 updated in Phase 3 to assert the real redirect-to-/login behavior (the old \"no-auth banner\" it originally checked no longer exists). Executed and passing."
      },
      {
        "id": "fnd-health-check",
        "feature": "Health-check endpoint (/api/health)",
        "status": "passing",
        "type": "manual + e2e (planned)",
        "notes": "Manually verified against a running `next start` in this environment: returns 200/'ok' when DB reachable, 503/'degraded' with checks.database='unreachable' when not \u2014 never crashes. tests/e2e/health.spec.ts codifies this but has not been run via Playwright's runner (see fnd-public-site-e2e)."
      }
    ]
  },
  "database": {
    "description": "Phase 2 (database) \u2014 Prisma schema, migration, and seed verification. Tracks the data layer directly; the per-module pub-*/adm-* entries below stay not_started until real UI/API code reads from these tables.",
    "items": [
      {
        "id": "db-schema-validate",
        "feature": "Prisma schema validates and formats cleanly (39 requested models + supporting tables)",
        "status": "passing",
        "type": "cli",
        "notes": "`npx prisma validate` and `npx prisma format` both clean against prisma/schema.prisma."
      },
      {
        "id": "db-migration-applied",
        "feature": "Migration applies to a real PostgreSQL database with zero drift",
        "status": "passing",
        "type": "manual",
        "notes": "PostgreSQL 16 installed locally (Homebrew) since none was available in this environment. Migration SQL generated statically via `prisma migrate diff --from-empty --to-schema=prisma/schema.prisma --script` (prisma/migrations/20260913114324_init_content_modules/), then applied for real with `prisma migrate deploy` \u2014 succeeded. `prisma migrate diff --from-config-datasource --to-schema=prisma/schema.prisma --exit-code` confirms zero drift afterward. A second migration (prisma/migrations/20260913121742_add_auth_sessions/) added the Session table and User lockout fields in Phase 3."
      },
      {
        "id": "db-seed-idempotent",
        "feature": "Seed script populates one demo record per module and is idempotent",
        "status": "passing",
        "type": "manual",
        "notes": "`npx prisma db seed` run repeatedly against the real database; row counts unchanged on repeat runs (all upserts). Seeds baseline roles, one placeholder college, one [DEV SEED] admin user, one [PLACEHOLDER] demo record per content module, and all 20 ComplianceRequirement rows transcribed from docs/compliance-matrix.md. Phase 4/homepage work found and fixed a real idempotency bug: several upserts' `update` branches were missing fields (status, startDate) that only `create` set, so re-seeding an already-existing row silently never published it or kept its date current \u2014 both are now mirrored into `update` too."
      },
      {
        "id": "db-compliance-verification-flow",
        "feature": "ComplianceRequirement -> ComplianceEvidence -> ComplianceVerification human-decision flow works and is not auto-derived",
        "status": "passing",
        "type": "manual",
        "notes": "Seeded one ComplianceEvidence + one ComplianceVerification (decision=VERIFIED) for requirement #4 (Faculty), then updated that requirement's status to VERIFIED. Verified via psql that only item #4 is VERIFIED and all other 19 remain NOT_STARTED/IN_PROGRESS (CLAUDE.md rule 7). The compliance workflow now built (see the `adm-compliance` entry) confirms this at the code level too: `syncAutomaticStatus` only ever moves a requirement between NOT_STARTED and IN_PROGRESS from evidence/completeness, and VERIFIED is reachable exclusively through the human-gated `verify` action."
      },
      {
        "id": "db-typecheck-lint-tests",
        "feature": "Existing app code still typechecks/lints/passes tests against the new generated Prisma Client",
        "status": "passing",
        "type": "cli",
        "notes": "`npm run typecheck`, `npm run lint`, `npm test` all clean after every schema change across phases."
      }
    ]
  },
  "auth": {
    "description": "Phase 3 (authentication & authorization) \u2014 login/logout, session handling, server-side permission checks, and the permission matrix (docs/permission-matrix.md). Source of truth for the matrix is src/lib/auth/permissions.ts.",
    "items": [
      {
        "id": "auth-permission-matrix",
        "feature": "Permission matrix is internally consistent (every role covered, separation of duties, governance/system-admin scoping)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/auth/permissions.test.ts."
      },
      {
        "id": "auth-password-hashing",
        "feature": "Password hashing/verification (bcryptjs, cost 12, unique salt per hash)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/auth/password.test.ts."
      },
      {
        "id": "auth-session-handling",
        "feature": "Server-side session store: token hashing, cookie flags (httpOnly/sameSite/secure-in-prod), expiry, revocation on logout",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/auth/session.test.ts \u2014 mocks Prisma and next/headers."
      },
      {
        "id": "auth-login-logout-actions",
        "feature": "Login/logout Server Actions: generic invalid-credentials error (no user enumeration), account lockout after repeated failures, audit logging (LOGIN/LOGIN_FAILED/LOGOUT)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/auth/actions.test.ts."
      },
      {
        "id": "auth-unauthenticated-access",
        "feature": "Unauthenticated access to /admin/* redirects to /login, server-side",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "tests/unit/auth/guard.test.ts + tests/e2e/auth.spec.ts (both run and pass; e2e verified against the real dev server and seeded database)."
      },
      {
        "id": "auth-authorized-access",
        "feature": "Authenticated user with the required permission reaches the page",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "tests/unit/auth/guard.test.ts, tests/unit/auth/actions.test.ts + tests/e2e/auth.spec.ts."
      },
      {
        "id": "auth-unauthorized-access",
        "feature": "Authenticated user lacking the required permission is redirected to /admin/unauthorized, server-side",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "tests/unit/auth/guard.test.ts + tests/e2e/auth.spec.ts."
      },
      {
        "id": "auth-role-specific-access",
        "feature": "Each of the 8 required roles (SUPER_ADMIN, PRINCIPAL, ADMINISTRATOR, EDITOR, REVIEWER, ADMISSION_OFFICER, EXAMINATION_OFFICER, FACULTY_EDITOR) gets exactly the routes its permission grants allow",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "tests/unit/auth/role-access.test.ts (matrix-level) + tests/e2e/auth.spec.ts (logs in as each seeded [DEV SEED] role account and checks real route access)."
      },
      {
        "id": "auth-route-permission-coverage",
        "feature": "Every ADMIN_NAV_LINKS route has a declared required permission, and every admin page.tsx actually calls requirePermission() for it",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/auth/route-permissions.test.ts checks the declared mapping; the mapping matches what each page.tsx calls (verified by code inspection, all 23 admin pages patched in the same change)."
      },
      {
        "id": "auth-login-rate-limit",
        "feature": "Login is rate-limited per IP (not just per-account lockout), so an attacker can't spread guesses across many accounts from one source without ever tripping any single account's lockout",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "Found during the 2026-09-15 security review: `User.failedLoginAttempts`/`lockedUntil` (auth-login-logout-actions above) only ever stops repeated guessing against *one* account. src/lib/auth/actions.ts now also calls the same per-IP `RateLimitEntry` infra the public grievance form already used (src/lib/security/rate-limit.ts), before the account lookup even runs. The threshold is environment-aware (10/15min in production; a much higher ceiling outside it) because this repo's own e2e suite opens nearly every spec with a real login, so a production-sized limit would make the test suite itself look like an attack from one IP (localhost) \u2014 see src/lib/auth/actions.ts's doc comment. tests/unit/auth/actions.test.ts covers the exceeded-limit path directly; the full e2e suite (which logs in dozens of times per run) re-passing end to end is itself evidence the non-production ceiling doesn't false-positive on legitimate rapid logins."
      }
    ]
  },
  "public_shell": {
    "description": "Phase 4 (public website shell) \u2014 shared layout/UI components (header, nav, mobile nav, footer, breadcrumbs, page container, buttons, cards, tables, alerts, empty states, loading states) and the src/lib/content.ts query layer they're built on. Per-section content depth is tracked in `public_website` below.",
    "items": [
      {
        "id": "shell-ui-primitives",
        "feature": "Shared UI primitives: Alert, EmptyState, DataTable, Breadcrumbs",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/ui/{Alert,EmptyState,Table,Breadcrumbs}.test.tsx."
      },
      {
        "id": "shell-content-queries",
        "feature": "src/lib/content.ts query layer only ever returns PUBLISHED rows (CLAUDE.md rule 4)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/content/queries.test.ts \u2014 mocks Prisma and asserts every checked query filters on status: \"PUBLISHED\" (plus isPublic: true for Results, and non-expired for the homepage's important-announcement query), and that a missing college short-circuits to an empty result without querying content tables. The former known gap (Document had no publish-state field, so /downloads listed every document for the college unconditionally) was closed in the Admissions/Examinations/Documents CMS session: Document now carries status/publishedAt/publishedBy like every other content model, and getDocuments() filters on status: \"PUBLISHED\"."
      },
      {
        "id": "shell-demo-data-publish-flow",
        "feature": "Seeded demo content is marked PUBLISHED (not bypassing the gate) and rendered with a visible DemoDataNotice",
        "status": "passing",
        "type": "manual + e2e",
        "notes": "prisma/seed.ts sets status: \"PUBLISHED\" + isPlaceholder: true on every content row it creates (mirrored into each upsert's `update` branch too, so re-seeding doesn't silently leave stale DRAFT rows or stale dates \u2014 see db-seed-idempotent). tests/e2e/public-content.spec.ts confirms both the content and the \"Demo content\" notice actually render."
      },
      {
        "id": "shell-responsive-no-horizontal-scroll",
        "feature": "No page-level horizontal scroll at mobile width; desktop header nav scrolls within its own bounded box instead of overflowing",
        "status": "passing",
        "type": "manual",
        "notes": "Found and fixed during Phase 4: the desktop nav's overflow-x-auto had no min-w-0/flex-1 boundary, so it silently pushed the whole header (and page) wider than the viewport instead of scrolling. Verified via Playwright screenshots at 1280px and 390px widths, plus `document.documentElement.scrollWidth <= clientWidth` at both \u2014 re-verified for the homepage's 15 sections."
      },
      {
        "id": "shell-grievance-encryption",
        "feature": "Grievance submission encrypts submitterContact before it reaches the database (CLAUDE.md rule 6)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/security/crypto.test.ts (AES-256-GCM round-trip, tamper detection, missing-key error) + tests/unit/grievance/actions.test.ts (routes the raw contact string through encryptSecret rather than writing it directly)."
      },
      {
        "id": "shell-accessibility-audit",
        "feature": "Site-wide accessibility audit (keyboard nav, focus management, semantic HTML, headings, labels, form errors, table accessibility, alt text, dialogs, buttons/links, responsive behavior, contrast) with fixes, not just findings",
        "status": "passing",
        "type": "e2e + manual",
        "notes": "New tests/e2e/accessibility.spec.ts (34 tests) runs axe-core (@axe-core/playwright) against every page \"shape\" \u2014 public content pages, login, admin dashboard/list/create/detail \u2014 plus every one of the 17 admin detail pages whose <dl> markup was fixed (see below), gated on WCAG 2.1 A/AA tags; all 34 passing. Real issues found and fixed, not just reported: (1) systemic contrast failure \u2014 text-foreground/50 measured 3.41:1 in light mode (fails WCAG AA's 4.5:1 for normal text; verified by computing relative luminance against the actual design tokens in globals.css), used across ~90 files (badges, form \"(optional)\" hints, help text) \u2014 replaced with text-foreground/60 (4.67:1 light / 6.36:1 dark, both passing) everywhere. (2) AdminSidebar, the public header's desktop nav, and MobileNav never indicated the current page \u2014 no aria-current, no visual distinction \u2014 across 38 admin links and the public nav; converted to Client Components (usePathname) with aria-current=\"page\" plus a visual active state (src/components/layout/{AdminSidebar,DesktopNavLinks,MobileNav}.tsx; DesktopNavLinks is new, extracted from PublicHeader). (3) 17 admin [id]/page.tsx detail pages had a <dl> containing a <div> that wrapped further <div>s around <dt>/<dd> pairs \u2014 invalid per the HTML definition-list content model (axe's definition-list/dlitem rules, both WCAG-tagged): academic-calendar, admissions, affiliation, audit-logs, college-profile, compliance, documents, enrollment-statistics, events, exams, fee-structures, gallery item, location, notices, results, seminars, workshops. Fixed by flattening each pair to dt/dd as direct grid children (`grid sm:grid-flow-col sm:grid-rows-2 sm:grid-cols-N`), which reproduces the original side-by-side visual layout without the invalid nesting; verified structurally (a script re-scanned all 32 <dl>-bearing files for the pattern: zero remaining) and via axe on each fixed page directly. (4) DataTable's horizontally-scrollable wrapper and two raw JSON <pre> blocks (audit log detail, timetable schedule) were scrollable but not keyboard-focusable (axe scrollable-region-focusable, serious impact) \u2014 added tabIndex={0}. First attempt also added role=\"region\" aria-label={caption} to DataTable, which then collided with an identically-named parent <section> landmark on pages like /academics and /admissions (axe landmark-unique) \u2014 caught by re-running a broader axe best-practice sweep after the fix, and corrected by dropping the redundant region role (a scrollable container only needs tabIndex, not its own landmark). (5) /login had no <main> landmark at all (its own top-level route, outside both the (public) and /admin layouts that normally provide one) \u2014 which also meant the site's skip link pointed at a target that didn't exist on that specific page; added <main id=\"main-content\">. (6) AdminUserBar (the \"Signed in as \u2026 / Sign out\" bar) rendered as a bare <div>, outside any landmark; changed to <header>. Manually verified and left unchanged, already correct: alt text on every raw <img> (FilePreviewInput, gallery item/album, document preview \u2014 all real, non-empty); no clickable <div>/<span> anti-patterns anywhere in the codebase; Alert's role=\"alert\" (danger/warning, assertive) vs role=\"status\" (info/success, polite) split; all color-token combinations (success/warning/danger/info/placeholder/brand backgrounds with their paired foreground) measured 6:1\u201312:1, well above AA; Breadcrumbs/Pagination already use aria-current=\"page\" correctly; MobileNav already had Escape-to-close plus aria-expanded/aria-controls; no true modal dialogs exist anywhere in the app (only MobileNav's non-modal disclosure panel), so there was nothing requiring a focus trap to audit; viewport meta is Next.js's unmodified default (no user-scalable=no/maximum-scale lockout); DataTable already used a real <table> with scope=\"col\" headers and a caption. tsc/eslint clean and the full 625/625 unit + full e2e regression re-run after all fixes."
      },
      {
        "id": "shell-security-review",
        "feature": "Site-wide security review (authentication, authorization, role escalation, server-side permissions, input validation, XSS, SQL injection, CSRF, file uploads, path traversal, session handling, secrets, API endpoints, private grievance data, admin routes, security headers, rate limiting) with fixes, not just findings",
        "status": "passing",
        "type": "unit + e2e + manual",
        "notes": "Full review, 2026-09-15. Most categories were already solid and are noted as verified, not re-implemented: authorization (every one of ~140 admin page.tsx/actions.ts files independently calls requireUser/requirePermission \u2014 verified by a script splitting every actions.ts on its exported-function boundaries and checking each one's own body, not just that the file contains the string somewhere); role assignment validates the target role against a fixed enum and is itself gated on users:manage, with no client-trusted role/permission ever accepted (the session cookie is an opaque random token, resolved from the database on every request \u2014 src/lib/auth/session.ts); SQL injection (Prisma is parameterized everywhere; the only raw query is `SELECT 1` in the health check, no interpolation); XSS (React auto-escapes everywhere; the one dangerouslySetInnerHTML \u2014 JSON-LD \u2014 already `<`-escapes, src/lib/seo.ts); CSRF (Next.js Server Actions verify Origin against Host automatically; no custom mutating Route Handlers exist, only GET file-serving/health routes); path traversal (every stored-file read already resolves and checks containment against its storage root, and every path segment comes from a DB-generated id or a randomUUID-prefixed sanitized filename, never raw user input); secrets (.env is gitignored, .env.example has no real values, GRIEVANCE_ENCRYPTION_KEY/DATABASE_URL are validated for presence/format in src/lib/env.ts, dev-seed credentials are never seeded when NODE_ENV=production); private grievance data (encrypted at rest, decrypted only on the permission-gated detail view, and the list query \u2014 while it does fetch the encrypted columns via Prisma's default `include` behavior \u2014 never renders them, verified by inspecting its DataTable columns). Real gaps found and fixed: (1) Login had per-account lockout but no per-IP rate limit, so an attacker could spread guesses across many different accounts from one source without ever tripping any single account's lockout \u2014 added the same RateLimitEntry-backed limiter the grievance form already used (see auth-login-rate-limit above). (2) File uploads (Document/Media/grievance attachments) validated only the client-asserted `File.type`, never the actual bytes \u2014 a scripted client can set any Content-Type on a multipart part it likes, so a disguised HTML/script file could be uploaded under an allow-listed MIME type and, for the inline-served Media route, risk a content-sniffing stored-XSS if a browser ever ignored the declared Content-Type. Added src/lib/security/file-signature.ts (magic-byte verification for every allow-listed type with a real signature \u2014 PDF/JPEG/PNG/GIF/WEBP/the legacy OLE2 Office formats/the ZIP-based OOXML formats) wired into both saveUploadedFile and saveGrievanceAttachment, so a mismatch is rejected before anything touches disk; tests/unit/security/file-signature.test.ts covers every type's accept/reject case directly, and the existing upload-storage/file-storage unit tests' fixtures were upgraded from placeholder text content to genuine magic-byte content they'd been implicitly relying on being irrelevant. (3) next.config.ts had zero security headers \u2014 added Content-Security-Policy (default-src 'self', explicit allow-lists per resource type, object-src/frame-ancestors 'none', no external origins since the app loads no third-party scripts/fonts/images), X-Content-Type-Options: nosniff, X-Frame-Options: DENY, Referrer-Policy, a locked-down Permissions-Policy, Strict-Transport-Security (production only), and poweredByHeader: false \u2014 verified present on a real `next start` response via curl, not just assumed from config. Known architectural gap, deliberately flagged rather than fixed: nearly every admin query filters records by `id` alone, not also by the signed-in user's `collegeId` \u2014 a real horizontal-authorization (IDOR) gap *if* this app ever became genuinely multi-tenant, but there is currently no code path anywhere that creates a second College row (verified: no prisma.college.create call exists, and the live dev database has exactly one), so today there is no other tenant's data to leak. Whether this app is single-tenant or a multi-tenant template is an explicit, still-open question (see progress.md); retrofitting collegeId scoping across ~30 modules' queries for a not-yet-decided requirement was judged out of scope for this review \u2014 see the decisions log. tsc/eslint clean; full 637/637 unit + full e2e regression (211/212 \u2014 the one failure is the same pre-existing, already-documented test-data-accumulation flake from shell-accessibility-audit's session, unrelated to any change here) re-run after every fix; a real `next start` production build was started and its response headers verified directly."
      },
      {
        "id": "shell-full-walkthrough",
        "feature": "Real browser-automation walkthrough of the whole product \u2014 the public site's 10 main entry points and the full admin notice lifecycle (login \u2192 dashboard \u2192 create \u2192 draft \u2192 submit \u2192 review \u2192 approve \u2192 publish \u2192 verify publicly \u2192 edit \u2192 audit trail) plus a compliance-dashboard verification, at both desktop and a real 375\u00d7812 mobile viewport",
        "status": "passing",
        "type": "e2e",
        "notes": "New tests/e2e/full-walkthrough.spec.ts (21 tests), run against the real dev server + seeded database with real browser interaction throughout \u2014 typing into the actual search box and clicking Search rather than navigating straight to `?q=`, following the real Edit link rather than calling a Server Action directly, etc. Found and fixed one real defect: at 375px width, the admin dashboard (`/admin`) overflowed horizontally. Root-caused by tracing computed widths up the DOM ancestor chain from the overflowing content rather than guessing \u2014 the \"Recent notices\"/\"Upcoming events\" cards sit in a `grid gap-6 lg:grid-cols-2` (single implicit column below `lg:`), and a CSS Grid item's default `min-width: auto` means it won't shrink below its content's intrinsic width unless told to; a long notice/event title inside a `flex items-center justify-between` row (with a `truncate` class the original author clearly intended to engage) never actually got the chance to truncate, because the *grid item* (the Card) itself, not just the text link, needed `min-w-0` to be allowed to shrink in the first place \u2014 a second, independent instance of the same flexbox/grid \"intrinsic minimum size\" trap already documented once in this project for the public header's nav (see the 2026-09-13 shell-responsive-no-horizontal-scroll decision). Fixed with `min-w-0` on both `<Card>`s (src/app/admin/page.tsx) plus `min-w-0 flex-1` on the two notice/event title `<Link>`s inside them (the latter alone was insufficient by itself \u2014 verified by measuring the DOM at each fix step rather than assuming one change was enough). Re-verified via direct `document.documentElement.scrollWidth`/`clientWidth` measurement (equal after the fix, previously 434 vs 375) as well as the full walkthrough spec passing end to end at 375px. tsc/eslint clean; full 637/637 unit + full e2e regression (232/233 \u2014 the one failure is the same pre-existing, already-documented test-data-accumulation flake, unrelated) re-run after the fix; a real production build (`npm run build`) verified clean."
      },
      {
        "id": "shell-public-redesign",
        "feature": "Public-site premium editorial design system, kept fully separate from the admin portal's shared plain/functional components",
        "status": "passing",
        "type": "e2e",
        "notes": "New src/components/public/** primitives + redesigned PublicHeader/PublicFooter/PublicPageShell/all ~16 homepage sections (docs/public-design-system.md has the full writeup). Admin portal (src/components/ui/**, src/components/admin/**, every /admin/* route) deliberately untouched \u2014 verified via the full tests/e2e/accessibility.spec.ts admin-page suite (still green) and a direct screenshot comparison. Found and fixed 4 real bugs via actual browser testing that tsc/eslint could not catch: (1) a Server Component passing a render-prop function to a Client Component (HeaderScrollShell) \u2014 invalid in RSC, threw at runtime, fixed via a data-scrolled attribute + CSS variants; (2) ArrowLink/CTAButton's own hardcoded `inline-flex` base class beating an externally-passed `hidden` override regardless of breakpoint, keeping the mobile header's Admissions button and SectionHeading's desktop View-all link visible on mobile \u2014 fixed by wrapping in a plain element instead of overriding the component's own display class; (3) two real WCAG contrast failures (axe-core) on --pub-ink-muted and --pub-gold-600 against the darker of two similar cream tokens, both just under AA's 4.5:1 \u2014 darkened with margin, verified via a script computing the real contrast formula; (4) several `aria-labelledby` references pointing at an `id` SectionHeading never actually set, plus one section (Principal's Message) with no heading at all \u2014 fixed by making `id` a required SectionHeading prop (tsc now catches a missing one) and adding a real heading. Also decided the public site is a deliberately fixed light theme (not mirroring prefers-color-scheme like the admin portal does) after an initial dark-mode token pass produced unreadable low-contrast text once tested under a real dark-OS environment. typecheck/lint clean; 650/650 unit tests; every e2e spec this work touches (homepage, public-content, public-site, auth, search, cms-location, full-walkthrough, accessibility \u2014 34/34 covering all 10 public + 24 admin pages) passes 100% in isolation. A handful of stale-seed-string literal assertions in those specs (left over from this same session's earlier college-dataset seed work, not from this redesign) were updated to match the real current content rather than weakened. Not done in this pass (see progress.md Next steps): the ~19 other public inner pages' own body content, still rendering through shared ui/* components below the now-elevated PublicPageShell intro band."
      }
    ]
  },
  "homepage": {
    "description": "Homepage implementation \u2014 15 required sections (src/components/home/*.tsx), each independently streamed and database-backed, plus the mobile/accessibility/performance/SEO optimizations that came with it.",
    "items": [
      {
        "id": "home-15-sections",
        "feature": "All required homepage sections render, each reading from src/lib/content.ts (no hard-coded institutional content)",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/homepage.spec.ts's \"renders every required section\" test \u2014 updated 2026-09-16 alongside the public-site redesign (docs/public-design-system.md): several sections were renamed/merged/restyled with new editorial headline copy (no longer a literal \"15 sections\" list), and two new sections (Why Choose Us, Principal's Message) were added. See the shell-public-redesign entry above for the full redesign writeup."
      },
      {
        "id": "home-announcement-filtering",
        "feature": "Important announcement banner shows the latest non-expired notice and excludes expired ones",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "getImportantAnnouncement in tests/unit/content/queries.test.ts + tests/e2e/homepage.spec.ts (seed includes one still-current and one deliberately-expired demo notice to prove the filter actually excludes something)."
      },
      {
        "id": "home-upcoming-events-filtering",
        "feature": "Upcoming events only shows events that haven't happened yet, soonest first",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "getUpcomingEvents in tests/unit/content/queries.test.ts + tests/e2e/homepage.spec.ts (seed's demo event is deliberately 30 days out, re-set on every seed run so it can't drift into the past)."
      },
      {
        "id": "home-suspense-streaming",
        "feature": "Every section below the hero streams independently via <Suspense>, so one slow query can't block the rest of the page (performance)",
        "status": "passing",
        "type": "manual",
        "notes": "src/app/(public)/page.tsx wraps each of the 14 non-hero sections in its own <Suspense fallback={<SectionSkeleton />}>; Hero itself is awaited directly (fast, above-the-fold, wants to be in the initial HTML for LCP/SEO) rather than streamed."
      },
      {
        "id": "home-accessibility-skip-link",
        "feature": "A skip link is the first focusable element and actually moves keyboard focus to #main-content",
        "status": "passing",
        "type": "e2e",
        "notes": "src/components/SkipLink.tsx. Found during e2e testing: Chromium scrolls to a same-page hash target but does not reliably move focus there for a tabindex=-1 element \u2014 a client-side onClick handler explicitly calls .focus() to close that gap. tests/e2e/homepage.spec.ts."
      },
      {
        "id": "home-seo-metadata",
        "feature": "Homepage has a database-driven (non-generic) <title>/description, canonical URL, and Open Graph/Twitter tags, without double-applying the site-wide title template",
        "status": "passing",
        "type": "e2e",
        "notes": "src/app/(public)/page.tsx's generateMetadata. Found and fixed during this phase: returning a plain string title let the root layout's `template: \"%s | <site>\"` apply a second time, producing \"<site> | <site>\" \u2014 fixed with `title: { absolute: title }`. tests/e2e/homepage.spec.ts."
      },
      {
        "id": "home-seo-structured-data",
        "feature": "Organization JSON-LD is only published once real (non-placeholder) college data exists \u2014 never presents demo data as fact to a crawler",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "src/lib/seo.ts's buildCollegeJsonLd, tested in tests/unit/seo.test.ts (field-by-field placeholder filtering) and tests/e2e/homepage.spec.ts (confirms zero JSON-LD script tags while the seeded college is still isPlaceholder)."
      },
      {
        "id": "home-seo-sitemap-robots",
        "feature": "sitemap.xml and robots.txt are generated from the same PUBLIC_NAV_LINKS table the nav uses, and exclude /admin and /login",
        "status": "passing",
        "type": "unit + e2e",
        "notes": "src/app/sitemap.ts, src/app/robots.ts, tested in tests/unit/sitemap-robots.test.ts and reachability-checked in tests/e2e/homepage.spec.ts."
      },
      {
        "id": "home-mobile-no-overflow",
        "feature": "No page-level horizontal scroll at mobile width (375px) across all 15 sections",
        "status": "passing",
        "type": "e2e + manual",
        "notes": "tests/e2e/homepage.spec.ts's responsiveness check (document.documentElement.scrollWidth) plus manual Playwright screenshots at 390px and 1280px."
      }
    ]
  },
  "cms_modules": {
    "description": "CMS modules for College Profile, Departments, Programs, Faculty, Staff, Notices, Events, Seminars, Workshops, Academic Calendar, Timetables, Admissions, Fee Structures, Enrollment Statistics, Examinations, Results, Documents, Infrastructure, Activities, Clubs, Gallery, Scholarships, Student Support, Policies, Regulations, Affiliation, Contact, and Location \u2014 the first real admin-side content authoring in the project (everything before this was PagePlaceholder). Each module has list/view/create/edit pages plus the full content approval workflow (DRAFT -> SUBMITTED -> UNDER_REVIEW -> APPROVED -> PUBLISHED, and PUBLISHED -> UPDATE_REQUIRED -> DRAFT \u2014 see the cms-workflow-engine entry), sharing one generic engine (src/lib/content-workflow.ts) and one audit-log writer (src/lib/audit.ts) rather than reimplementing either 28 times. Notices/Events/Seminars/Workshops/Academic Calendar/Timetables/Admissions/Examinations/Results/Affiliation also share src/lib/admin/zod-helpers.ts for date-field validation/conversion. Admissions/Fee Structures/Enrollment Statistics are the first modules in the `content_admissions` domain and Examinations/Results the first in `content_examinations`; Clubs is the third `content_faculty` module (alongside Faculty/Staff). Documents closed the long-tracked Document publish-gate gap; this batch similarly added `publishedAt`/`publishedBy` to `Contact` and `Location`, which (like `FeeStructure`/`EnrollmentStatistic`/`Result` before them) had `status` but not the publish-timestamp columns the shared workflow engine needs. Gallery is the first nested-resource module (GalleryAlbum -> GalleryItem, where adding an item also creates a fresh Media asset in the same action) and Location is the second true singleton module (after College Profile), using `findFirst` rather than `findUnique` since `Location.collegeId` has no `@unique` constraint.",
    "items": [
      {
        "id": "cms-workflow-engine",
        "feature": "Content approval workflow state machine: DRAFT -> SUBMITTED -> UNDER_REVIEW -> APPROVED -> PUBLISHED, and PUBLISHED -> UPDATE_REQUIRED -> DRAFT. Every transition enforces the caller's permission (manage for submit_for_review/return_to_draft, publish for start_review/approve/reject/publish/request_update), is refused if illegal from the record's current status, stores the actor + a timestamp + an optional comment/reason on the audit log entry it writes, and rejection specifically requires a non-empty reason. Only PUBLISHED content is ever publicly visible (src/lib/content.ts's `isPubliclyVisible` / status: \"PUBLISHED\" filters).",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/content-workflow.test.ts \u2014 32 tests covering every one of the 7 transitions (parameterized), illegal-transition rejection (WorkflowError), the rejection-requires-a-reason rule (empty and whitespace-only both refused), comment storage on both reject and non-reject transitions, publishedAt/publishedBy set only on publish, getAvailableActions' permission filtering for every status, and isPubliclyVisible. Superseded the prior DRAFT/PENDING_REVIEW/APPROVED/PUBLISHED/ARCHIVED engine via prisma/migrations/20260914150000_content_approval_workflow_v2 (applied against the real local Postgres instance with zero drift; no ARCHIVED/PENDING_REVIEW rows existed in seeded data, so the remap was a schema-only change in practice). All 28 modules' actions.ts/page.tsx were mechanically updated to the new action set and to plumb a shared comment field + workflowError display through WorkflowActions."
      },
      {
        "id": "cms-audit-logging",
        "feature": "Every CREATE/UPDATE mutation (and every workflow transition) writes a CLAUDE.md rule 8 audit trail entry, with Date fields serialized to plain JSON",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/audit.test.ts. This is the first real (non-login/logout) writer of AuditLog in the project. Now also covers getLatestActionComment (4 new cases: happy path, query shape, null when no match, actor-missing fallback label)."
      },
      {
        "id": "cms-workflow-update-reason-banner",
        "feature": "The reviewer's request_update reason is surfaced as a visible banner directly on a record's own admin page when its status is UPDATE_REQUIRED, instead of only being discoverable via the audit trail (progress.md open question, resolved 2026-09-16). WorkflowActions (src/components/admin/WorkflowActions.tsx) is an async Server Component that looks up the latest REQUEST_UPDATE AuditLog entry for the record via the new src/lib/audit.ts getLatestActionComment() helper and renders it as a warning Alert. Wired into all 29 ContentStatus-workflow call sites (every [id]/page.tsx and the two singleton pages, College Profile and Location) via a new required entityType prop matching each module's own applyWorkflowTransition entityType string; GrievanceWorkflowActions (a separate component for the non-publishing grievance case-management lifecycle) is deliberately untouched.",
        "status": "passing",
        "type": "unit+e2e",
        "notes": "Unit: tests/unit/audit.test.ts (see cms-audit-logging above). E2E: tests/e2e/cms-notices.spec.ts, extended \u2014 the existing \"REVIEWER can flag ... as needing an update\" test now fills a reason, plus a new test asserting the banner (with the exact reason text) renders on the record's own page for a different role (EDITOR) afterward. Full spec run: 8/8 passing."
      },
      {
        "id": "cms-module-permission-mapping",
        "feature": "Each module maps to the correct permission domain (College Profile/Departments/Programs -> content_general; Faculty/Staff -> content_faculty), and no role except SUPER_ADMIN can both manage and publish the same module (structural no-self-approval)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/module-permissions.test.ts \u2014 cross-checks src/lib/admin/module-permissions.ts against the live ROLE_PERMISSIONS matrix from Phase 3, so the two can never silently drift apart."
      },
      {
        "id": "cms-departments-full-lifecycle-e2e",
        "feature": "End-to-end cross-role lifecycle: EDITOR creates+submits, REVIEWER approves+publishes+archives, ADMINISTRATOR is view-only, FACULTY_EDITOR is blocked (wrong domain), and the published department appears on the public /academics page",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-departments.spec.ts (10 tests) \u2014 the strongest evidence 'apply role permissions' holds against the real dev server + seeded database, not just mocked unit tests. Found and fixed two real e2e bugs while writing this: (1) a `waitForURL` regex that also matched the literal \"/new\" route segment, capturing a stale URL; (2) `waitForURL(sameUrl)` is a no-op whenever a Server Action redirects back to the page it started from, so it never actually waited for the mutation \u2014 replaced with content-based waits (a generously-timed assertion on the resulting status text)."
      },
      {
        "id": "cms-faculty-domain-e2e",
        "feature": "Faculty module exercises the content_faculty domain distinctly from Departments' content_general domain, plus the department-FK select and subjectsTaught array field",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-faculty.spec.ts (5 tests): FACULTY_EDITOR creates, PRINCIPAL (not REVIEWER, to also prove PRINCIPAL's content_faculty:publish grant) approves+publishes, EDITOR is blocked (wrong domain)."
      },
      {
        "id": "cms-departments-actions-unit",
        "feature": "Departments create/update/transition Server Actions: correct permission per action, validation, audit logging, race-condition safety",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/departments/actions.test.ts (11 tests) \u2014 the template the other 4 modules' tests follow."
      },
      {
        "id": "cms-programs-actions-unit",
        "feature": "Programs create/update/transition: department-FK validation (including cross-college rejection) and ProgramLevel enum validation",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/programs/actions.test.ts."
      },
      {
        "id": "cms-faculty-actions-unit",
        "feature": "Faculty create/update/transition: comma-separated subjectsTaught parsed to an array, email format validation, content_faculty (not content_general) permission checks",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/faculty/actions.test.ts."
      },
      {
        "id": "cms-staff-actions-unit",
        "feature": "Staff create/update/transition: simplest module (no FK), optional plain-string department field",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/staff/actions.test.ts."
      },
      {
        "id": "cms-college-profile-actions-unit",
        "feature": "College Profile create/update/transition: singleton behavior (create redirects to edit if a profile already exists; update looks the row up by collegeId, not a submitted id)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/college-profile/actions.test.ts."
      },
      {
        "id": "cms-date-helpers-unit",
        "feature": "Shared requiredDateField/optionalDateField Zod validators and toDateInputValue formatter (src/lib/admin/zod-helpers.ts) used by every date-bearing module below",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/zod-helpers.test.ts (8 tests) \u2014 valid/invalid/missing dates for both required and optional variants, plus the date->HTML-input-value round trip."
      },
      {
        "id": "cms-notices-full-lifecycle-e2e",
        "feature": "Notices: end-to-end cross-role lifecycle including date fields (publishDate/expiryDate) \u2014 EDITOR creates+submits, REVIEWER approves+publishes+archives, ADMINISTRATOR is view-only, FACULTY_EDITOR is blocked (wrong domain), and the published notice appears on the public /notices page",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-notices.spec.ts (8 tests, all passing) \u2014 first CMS e2e spec with date-field inputs. Also covers the update-requested reason banner (cms-workflow-update-reason-banner)."
      },
      {
        "id": "cms-timetables-full-lifecycle-e2e",
        "feature": "Timetables: end-to-end lifecycle exercising a required Program FK, an optional JSON structuredSchedule field (with malformed-JSON rejection on edit), workflow through publish, and public visibility on /academics; also confirms a wrong-domain role (ADMISSION_OFFICER) cannot create one",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-timetables.spec.ts (6 tests, all passing; verified stable across 3 consecutive full-suite runs). Found and fixed two real e2e bugs while writing this: (1) ambiguous `getByLabel` matchers (e.g. \"Program\" also matching the sidebar's \"Programs\" nav link) silently targeted the wrong element \u2014 replaced with direct `#id` locators; (2) capturing `page.url()` right after a content-based `expect(...).toBeVisible()` could still read the stale `/new` URL, because during a Next.js client-side transition the new page's DOM can attach a tick before the address bar/history commits \u2014 fixed with `page.waitForURL((url) => !url.pathname.endsWith(\"/new\"))` before reading `page.url()`. Also found a distinct bug in the test itself (not the app): logging in as a second role (`loginAs(page, \"reviewer\")`) on the same page/context right after a prior `loginAs(page, \"editor\")` call hung, because the app redirects an already-authenticated session away from /login before the login form ever renders \u2014 fixed by splitting the editor and reviewer steps into separate `test()` blocks (each gets a fresh context), matching the pattern already used in cms-notices.spec.ts and cms-departments.spec.ts."
      },
      {
        "id": "cms-notices-actions-unit",
        "feature": "Notices create/update/transition: required title/body, optional category/publishDate/expiryDate, invalid-date rejection",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/notices/actions.test.ts."
      },
      {
        "id": "cms-events-actions-unit",
        "feature": "Events create/update/transition: required startDate vs. optional endDate",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/events/actions.test.ts."
      },
      {
        "id": "cms-seminars-actions-unit",
        "feature": "Seminars create/update/transition: optional Department FK (omitted, valid, and cross-college-rejected cases)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/seminars/actions.test.ts."
      },
      {
        "id": "cms-workshops-actions-unit",
        "feature": "Workshops create/update/transition: same optional-Department-FK shape as Seminars, with facilitator instead of speaker",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/workshops/actions.test.ts."
      },
      {
        "id": "cms-academic-calendar-actions-unit",
        "feature": "Academic Calendar create/update/transition: no FK, plain category/academicYear string fields",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/academic-calendar/actions.test.ts."
      },
      {
        "id": "cms-timetables-actions-unit",
        "feature": "Timetables create/update/transition: required Program FK (including cross-college rejection), structuredSchedule JSON validation (malformed JSON rejected, valid JSON parsed and stored, omitted schedule left undefined rather than invented)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/timetables/actions.test.ts."
      },
      {
        "id": "cms-admissions-full-lifecycle-e2e",
        "feature": "Admissions: first e2e coverage of the content_admissions domain \u2014 ADMISSION_OFFICER creates+submits, REVIEWER approves+publishes, ADMINISTRATOR is view-only, EXAMINATION_OFFICER (wrong domain) is blocked, and the published admission cycle appears on the public /admissions page",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-admissions.spec.ts (6 tests, all passing; verified stable across 3 consecutive full-suite runs)."
      },
      {
        "id": "cms-results-double-gate-e2e",
        "feature": "Results: first e2e coverage of the content_examinations domain, and a direct demonstration of Result's double publish gate \u2014 a result reaches the public /results page only once it is BOTH status: PUBLISHED AND isPublic: true, neither flag implying the other",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-results.spec.ts (7 tests, all passing) \u2014 creates a result with a unique externalLink, publishes it while isPublic stays false and confirms it is still absent from /results, then (as the EXAMINATION_OFFICER who authored it, proving 'editing doesn't revert status' from the 2026-09-14 decisions log) edits it to set isPublic: true and confirms it now appears. Also confirms ADMISSION_OFFICER (wrong domain) cannot create a result."
      },
      {
        "id": "cms-admissions-actions-unit",
        "feature": "Admissions create/update/transition: required Program FK (including cross-college rejection), optional eligibilityCriteria/applicationStartDate/applicationEndDate",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/admissions/actions.test.ts."
      },
      {
        "id": "cms-fee-structures-actions-unit",
        "feature": "Fee Structures create/update/transition: required Program FK, optional Admission FK (omitted, valid, and cross-college-rejected cases), Decimal amount validation (rejects non-positive and non-numeric amounts), currency defaults to PKR when omitted",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/fee-structures/actions.test.ts. Prisma's Decimal type can't cross the Server Component -> Client Component boundary as-is (it's a class instance, not a plain serializable value) \u2014 FeeStructureForm.tsx accepts a pre-serialized `amount: string` and the edit page converts it with `.toString()` before passing it down; found and fixed while building this module, generalizable to any future Decimal-bearing form."
      },
      {
        "id": "cms-enrollment-statistics-actions-unit",
        "feature": "Enrollment Statistics create/update/transition: required Program FK, required totalEnrolled, optional maleCount/femaleCount left null (not invented as zero) when blank",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/enrollment-statistics/actions.test.ts. Found a real Zod pitfall while building this: `z.coerce.number().min(0).optional().or(z.literal(\"\"))` silently turns a blank field into 0 rather than falling through to the empty-string branch, because `Number(\"\") === 0` already satisfies `min(0)` before the literal(\"\") alternative is ever tried (unlike the pre-existing establishedYear precedent, whose min(1800) bound reliably fails for a coerced-to-0 blank value) \u2014 fixed by passing `formData.get(x) || undefined` into a plain `.optional()` schema instead of routing a literal empty string through the number coercion at all. This is a CLAUDE.md rule 1 correctness fix (never invent data), not just a style choice."
      },
      {
        "id": "cms-examinations-actions-unit",
        "feature": "Examinations create/update/transition: required Program FK, optional Notice FK (omitted, valid, and cross-college-rejected cases), optional academicYear/scheduleStartDate/scheduleEndDate",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/exams/actions.test.ts."
      },
      {
        "id": "cms-results-actions-unit",
        "feature": "Results create/update/transition: required Program + Examination FKs (cross-college rejection for both), isPublic checkbox (present as \"on\" when checked, absent when unchecked, never invented), externalLink URL validation",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/results/actions.test.ts."
      },
      {
        "id": "cms-documents-actions-unit",
        "feature": "Documents create/update/transition: required title/fileUrl (URL-validated), optional category/mimeType/sizeBytes (left null, not invented as 0, when blank), stamps a `(\"College\", collegeId)` entityType/entityId sentinel for these general (not-attached-to-another-record) downloads and records the creator as uploadedBy",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/documents/actions.test.ts. This is the first module built on Document, which previously had no status/publishedAt/publishedBy fields at all \u2014 added via prisma/migrations/20260914090529_add_workflow_fields_admissions_documents (applied with zero drift), closing the shell-content-queries known gap. prisma/seed.ts's dev-seed-document was updated to set status: \"PUBLISHED\" to match every other seeded content row, so it doesn't silently vanish from /downloads under the new gate."
      },
      {
        "id": "cms-gallery-full-lifecycle-e2e",
        "feature": "Gallery: the first e2e coverage of a nested-resource CMS module \u2014 EDITOR creates an album, adds an item (which creates a fresh Media asset attached to the album in the same action), submits both for review, REVIEWER approves+publishes both, and both appear on the public /gallery page; also confirms FACULTY_EDITOR (wrong domain) cannot create an album",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-gallery.spec.ts (6 tests, all passing; verified stable across 3 consecutive full-suite runs) \u2014 passed cleanly on the first run, no new e2e bugs found."
      },
      {
        "id": "cms-location-singleton-e2e",
        "feature": "Location: the second singleton-module e2e coverage (after College Profile), run against the already-PUBLISHED seeded location rather than a from-scratch create \u2014 confirms the /new page redirects away (proving the findFirst-based singleton guard), that editing a PUBLISHED location doesn't revert its status, and that the edit is reflected on the public /campus page; also confirms FACULTY_EDITOR (wrong domain) is blocked",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-location.spec.ts (4 tests, all passing). One test-authoring bug found and fixed while writing this: the test initially expected `/admin/location/new` to redirect to `/admin/location/edit`, but the established College Profile convention (confirmed by re-reading its `new/page.tsx`) redirects a page-load \"already exists\" guard to the plain *view* page, not the edit page \u2014 only the create Server Action itself (invoked with stale form state) redirects to edit. The application code was already correct and consistent with that convention; only the test's expectation was wrong."
      },
      {
        "id": "cms-infrastructure-actions-unit",
        "feature": "Infrastructure create/update/transition: required InfrastructureCategory enum (rejects an invalid category), no FK",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/infrastructure/actions.test.ts."
      },
      {
        "id": "cms-activities-actions-unit",
        "feature": "Activities create/update/transition: no FK, plain optional category field",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/activities/actions.test.ts."
      },
      {
        "id": "cms-clubs-actions-unit",
        "feature": "Clubs create/update/transition: optional Faculty FK as advisor (omitted, valid, and cross-college-rejected cases), content_faculty domain (the third module in this domain, alongside Faculty/Staff)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/clubs/actions.test.ts."
      },
      {
        "id": "cms-gallery-actions-unit",
        "feature": "Gallery album create/update/transition (no FK) and item create/update/transition (required accessibility alt text, creates/updates a Media asset alongside the GalleryItem row in one action)",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/gallery/actions.test.ts \u2014 covers both the album and nested item Server Actions."
      },
      {
        "id": "cms-scholarships-actions-unit",
        "feature": "Scholarships create/update/transition: no FK, plain optional eligibility field",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/scholarships/actions.test.ts."
      },
      {
        "id": "cms-student-support-actions-unit",
        "feature": "Student Support create/update/transition: no FK, plain optional contactInfo field",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/student-support/actions.test.ts."
      },
      {
        "id": "cms-policies-actions-unit",
        "feature": "Policies create/update/transition: no FK, plain optional category/body fields",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/policies/actions.test.ts."
      },
      {
        "id": "cms-regulations-actions-unit",
        "feature": "Regulations create/update/transition: no FK, plain optional category/body/regulatingBody fields",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/regulations/actions.test.ts."
      },
      {
        "id": "cms-affiliation-actions-unit",
        "feature": "Affiliation create/update/transition: optional Program FK (omitted, valid, and cross-college-rejected cases), optional validFrom/validTo dates",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/affiliation/actions.test.ts."
      },
      {
        "id": "cms-contact-actions-unit",
        "feature": "Contact create/update/transition: required ContactType enum (rejects an invalid type), no FK \u2014 the first module exercising Contact's newly-added publishedAt/publishedBy columns",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/contact/actions.test.ts."
      },
      {
        "id": "cms-location-actions-unit",
        "feature": "Location create/update/transition: singleton behavior via findFirst (not findUnique, since Location.collegeId has no @unique constraint) \u2014 create redirects to the view page if a location already exists; update looks the row up by collegeId, not a submitted id; optional latitude/longitude left null (not invented as 0) when blank",
        "status": "passing",
        "type": "unit",
        "notes": "tests/unit/admin/location/actions.test.ts."
      },
      {
        "id": "cms-workflow-reject-reason-e2e",
        "feature": "Rejecting UNDER_REVIEW content through the real admin UI is refused without a reason (shown as an alert, status unchanged) and succeeds once the shared comment field is filled in, sending the record back to DRAFT",
        "status": "passing",
        "type": "e2e",
        "notes": "tests/e2e/cms-departments.spec.ts, \"Rejecting under-review content requires a reason (real browser UI)\" describe block (3 tests) \u2014 the first e2e coverage of WorkflowActions' single shared <textarea name=\"comment\"> submitted via multiple buttons' `formAction`, proving that mechanism actually works in a real browser, not just at the Server Action level."
      }
    ]
  },
  "public_website": [
    {
      "id": "pub-home",
      "feature": "Home (15 sections: Hero, Important announcement, Quick links, Latest notices, Upcoming events, College introduction, Academic programs, Departments, Facilities, Latest activities, Scholarships/Student support, Important documents, Grievance link, Location, Contact)",
      "status": "passing",
      "notes": "src/app/(public)/page.tsx + src/components/home/*.tsx \u2014 every section reads live, publish-gated content from src/lib/content.ts; each is independently streamed via <Suspense> so a slow query never blocks the rest of the page. Covered by tests/e2e/homepage.spec.ts (11 tests), tests/e2e/public-site.spec.ts, and tests/unit/content/queries.test.ts."
    },
    {
      "id": "pub-about",
      "feature": "About (College Profile, History, Vision/Mission, Principal's Message)",
      "status": "passing",
      "notes": "src/app/(public)/about/page.tsx \u2014 one CollegeProfile row rendered as 4 labeled sections (consolidates 4 of the original 29 routes into clearly-headed sections on one page, per progress.md's decisions log). Covered by tests/e2e/public-content.spec.ts (breadcrumb check)."
    },
    {
      "id": "pub-academics",
      "feature": "Academics (Departments, Programs, Academic Calendar, Timetables)",
      "status": "passing",
      "notes": "src/app/(public)/academics/page.tsx \u2014 4 DataTable sections from Department/Program/AcademicCalendar/Timetable (consolidates 4 of the original routes). Manually verified rendering via a Playwright screenshot at desktop and mobile width; no dedicated e2e assertion beyond that yet."
    },
    {
      "id": "pub-admissions",
      "feature": "Admissions (cycles, fee structure, enrollment statistics)",
      "status": "passing",
      "notes": "src/app/(public)/admissions/page.tsx \u2014 3 DataTable sections from Admission/FeeStructure/EnrollmentStatistic. tests/e2e/cms-admissions.spec.ts publishes a real admission cycle through the CMS and confirms it renders on this page by its academic year."
    },
    {
      "id": "pub-faculty",
      "feature": "Faculty",
      "status": "passing",
      "notes": "src/app/(public)/faculty/page.tsx \u2014 DataTable from Faculty + Department. Covered by tests/e2e/public-content.spec.ts and shell-content-queries."
    },
    {
      "id": "pub-staff",
      "feature": "Staff (non-teaching)",
      "status": "in_progress",
      "notes": "src/app/(public)/staff/page.tsx \u2014 DataTable from Staff. No page-specific e2e assertion yet."
    },
    {
      "id": "pub-campus",
      "feature": "Campus (Infrastructure, Location)",
      "status": "passing",
      "notes": "src/app/(public)/campus/page.tsx \u2014 Infrastructure DataTable + Location address/map (consolidates 2 of the original routes). tests/e2e/cms-location.spec.ts confirms an edited Location address renders here; Infrastructure itself still has no page-specific e2e assertion beyond the generic no-500 check."
    },
    {
      "id": "pub-notices",
      "feature": "Notices",
      "status": "passing",
      "notes": "src/app/(public)/notices/page.tsx \u2014 Notice list, newest first. Covered by tests/e2e/public-content.spec.ts."
    },
    {
      "id": "pub-events",
      "feature": "Events (Events, Seminars, Workshops, Activities, Clubs)",
      "status": "in_progress",
      "notes": "src/app/(public)/events/page.tsx \u2014 5 sections (consolidates Events + the former standalone Activities route plus Seminars/Workshops/Clubs, which had no route of their own before). Admin CMS modules now exist for all 5 underlying models (adm-activities, adm-clubs, plus the earlier adm-seminars/adm-workshops/adm-events), but no page-specific e2e assertion on this page itself yet beyond the generic no-500 check."
    },
    {
      "id": "pub-gallery",
      "feature": "Gallery",
      "status": "passing",
      "notes": "src/app/(public)/gallery/page.tsx \u2014 GalleryAlbum -> GalleryItem -> Media, each image rendered with required alt text, served via /api/files/media/[id] (permission-gated for anything not currently PUBLISHED). tests/e2e/cms-gallery.spec.ts publishes a real uploaded image through the CMS, confirms it renders here and is fetchable without a session, and confirms it stops being fetchable once unpublished."
    },
    {
      "id": "pub-examinations",
      "feature": "Examinations",
      "status": "in_progress",
      "notes": "src/app/(public)/examinations/page.tsx \u2014 DataTable from Examination + Program. The admin CMS module (adm-exams) can now publish real Examination rows; no page-specific e2e assertion on /examinations itself yet (only the generic no-500 check in public-site.spec.ts and the cms-results.spec.ts fixture examination, which isn't asserted against this page)."
    },
    {
      "id": "pub-results",
      "feature": "Results",
      "status": "passing",
      "notes": "src/app/(public)/results/page.tsx \u2014 DataTable from Result, gated on status: PUBLISHED AND isPublic: true (rule 4's strictest case: two independent flags both have to allow it). tests/e2e/cms-results.spec.ts directly proves the double gate: a PUBLISHED-but-not-public result is confirmed absent from this page, then confirmed to appear once isPublic is also set."
    },
    {
      "id": "pub-scholarships",
      "feature": "Scholarships",
      "status": "in_progress",
      "notes": "src/app/(public)/scholarships/page.tsx \u2014 card list from Scholarship. Admin CMS module now exists (adm-scholarships); no page-specific e2e assertion yet."
    },
    {
      "id": "pub-student-support",
      "feature": "Student Support",
      "status": "in_progress",
      "notes": "src/app/(public)/student-support/page.tsx \u2014 card list from StudentSupport. Admin CMS module now exists (adm-student-support); no page-specific e2e assertion yet."
    },
    {
      "id": "pub-rules",
      "feature": "Rules & Regulations (Policies, Regulations)",
      "status": "in_progress",
      "notes": "src/app/(public)/rules/page.tsx \u2014 2 sections (consolidates the former rules-regulations route's scope, now split by underlying model). Admin CMS modules now exist for both (adm-policies, adm-regulations); no page-specific e2e assertion yet."
    },
    {
      "id": "pub-affiliation",
      "feature": "Affiliation",
      "status": "in_progress",
      "notes": "src/app/(public)/affiliation/page.tsx \u2014 DataTable from Affiliation + Program. Admin CMS module now exists (adm-affiliation); no page-specific e2e assertion yet."
    },
    {
      "id": "pub-grievance",
      "feature": "Grievance",
      "status": "passing",
      "notes": "src/app/(public)/grievance/page.tsx + actions.ts \u2014 full public intake form (name, email, phone, category, subject, description, attachment), writing to Grievance with submitterEmail/submitterPhone encrypted at rest (rule 6) and a generated, non-guessable reference number (src/lib/grievance-reference.ts) shown to the submitter. Per-IP rate limiting (src/lib/security/rate-limit.ts, 3/hour) plus a honeypot field. Attachments validated (type/size) and stored outside public/ (src/lib/security/file-storage.ts). No listing of past submissions is ever rendered publicly. Covered by tests/unit/grievance/actions.test.ts, tests/unit/grievance/reference-number.test.ts, tests/unit/security/rate-limit.test.ts, tests/unit/security/file-storage.test.ts, tests/e2e/grievance.spec.ts, tests/e2e/public-content.spec.ts, and tests/e2e/homepage.spec.ts (grievance callout link)."
    },
    {
      "id": "pub-contact",
      "feature": "Contact",
      "status": "in_progress",
      "notes": "src/app/(public)/contact/page.tsx \u2014 Contact list + Location address (folds the former standalone Location route's address display in here too; Campus owns the map). Admin CMS module now exists (adm-contact); no page-specific e2e assertion yet."
    },
    {
      "id": "pub-downloads",
      "feature": "Downloads",
      "status": "passing",
      "notes": "src/app/(public)/downloads/page.tsx \u2014 DataTable from Document, filtered to status PUBLISHED and within its publishDate/expiryDate window (src/lib/content.ts's getDocuments/isDocumentPubliclyVisible). Download links point at /api/files/documents/[id] (real uploaded file, permission-gated when not currently public). Covered by tests/e2e/cms-documents.spec.ts (appears/disappears on publish, unpublish, archive, and future-publishDate scheduling; file downloads without a session when live, requires auth otherwise)."
    },
    {
      "id": "pub-search",
      "feature": "Search",
      "status": "passing",
      "notes": "src/app/(public)/search/page.tsx + searchSite() in src/lib/content.ts \u2014 global keyword search across 8 categories (pages [a static list mirroring PUBLIC_NAV_LINKS, enriched with descriptions \u2014 no Page table exists], notices, events, programs, faculty, documents [respecting the same publish/expiry window as /downloads], policies, regulations), all database categories filtered to PUBLISHED only (rule 4). Supports an optional category filter (?category=), pagination over the combined ranked result list (src/lib/pagination.ts + src/components/ui/Pagination.tsx, reused from the admin grievances module), a deterministic in-memory relevance score (exact title match > title-starts-with > whole-word title match > partial title match, +bonus if the snippet also matches, ties broken alphabetically \u2014 no full-text-search infra exists, so this ranks already-matched DB rows rather than replacing the `contains` query), and a real empty state. Plain GET form, works without JS. Covered by tests/unit/content/queries.test.ts (searchSite: category filtering, document date-window, relevance ordering, pagination math), tests/unit/ui/Pagination.test.tsx, tests/e2e/search.spec.ts (9 tests: every category represented, static pages, category filter + its own empty state, nonsense-query empty state, blank query, relevance ordering, pagination absent when unnecessary, draft content never searchable), and tests/e2e/public-content.spec.ts's original program-search smoke test."
    }
  ],
  "admin_system": [
    {
      "id": "adm-dashboard",
      "feature": "Dashboard",
      "status": "passing",
      "notes": "Real, database-derived dashboard (src/app/admin/page.tsx + src/lib/admin/dashboard.ts) behind requirePermission(\"dashboard:view\"). Summary cards: total published/draft/pending-review content (site-wide groupBy across all 28 content-authoring modules, summed only across modules the signed-in user holds :view on \u2014 never a site-wide leak to a narrowly-scoped role), compliance percentage (share of the 20 circular requirements actually VERIFIED by a human, not average completeness), requirements needing attention, and document expiry warnings. Tables: recent notices, upcoming events (both all-status, not just published \u2014 staff need to see their own pending work), compliance requirements needing attention (worst-status-first: NEEDS_UPDATE > READY_FOR_REVIEW > IN_PROGRESS > NOT_STARTED), content not recently reviewed (published content untouched for 180+ days, across the ~19 modules with an actual editorial title), document expiry warnings (expired or expiring within 30 days), and recent audit activity. Every section gated by the same permission its own admin module already enforces (compliance:view, audit_logs:view, content_general:view), so a role only ever sees real data for domains it can actually open \u2014 CLAUDE.md rule 5. No number is hard-coded; an empty database yields all zeros, never a fabricated placeholder value. Covered by tests/unit/admin/dashboard.test.ts (permission filtering, status aggregation, staleness threshold, compliance percent/priority ordering, expiry classification, audit actor fallback) and tests/e2e/dashboard.spec.ts (9 tests: per-role section visibility, live increments from real admin actions, and the full upload\u2192publish\u2192dashboard-expiry-warning path). Extended with a real \"Content review warnings\" section (content-review/freshness tracking task): 5 named stale-<module> stat cards (Notices, Faculty, Academic Calendar, Timetables, Admissions) plus a combined \"Overdue reviews\" table, all backed by real lastReviewedAt data and a configurable per-module review period \u2014 not the updatedAt heuristic the old \"content not recently reviewed\" table used (that table, renamed \"Other content not recently reviewed\", now covers only the remaining modules without real review tracking)."
    },
    {
      "id": "adm-cms",
      "feature": "CMS",
      "status": "in_progress",
      "notes": "Placeholder content, gated behind requirePermission(\"content_general:view\"). Draft vs. published state separation (CLAUDE.md rule 4) still to be implemented when the real CMS is built."
    },
    {
      "id": "adm-users",
      "feature": "Users",
      "status": "passing",
      "notes": "Real role-assignment UI at /admin/users (src/app/admin/users/page.tsx + src/app/admin/users/actions.ts), gated behind requirePermission(\"users:manage\"). Scoped deliberately to what \"permission changes\" means in this app's data model: which roles a UserRole row grants a user, since role->permission grants (ROLE_PERMISSIONS) are fixed in code/seed, not runtime-editable. assignRoleAction/removeRoleAction validate the role name, refuse assigning an already-held role, refuse removing a user's last role (would silently lock them out of every permission-gated page), and log a ROLE_CHANGE audit entry with before/after role lists and metadata (changeType, role, targetUserEmail) via the centralized logAudit() writer. Full CRUD user management (create/deactivate accounts) and the Roles/Permissions matrix editor remain out of scope for this pass \u2014 see adm-roles/adm-permissions. Covered by tests/unit/admin/users/actions.test.ts (7 cases) and tests/e2e/audit-logs.spec.ts's \"permission changes\" block (assign + remove, each verified against the real audit log detail page)."
    },
    {
      "id": "adm-roles",
      "feature": "Roles",
      "status": "in_progress",
      "notes": "Placeholder content, gated behind requirePermission(\"roles:manage\"). Role table now seeded with the 8 required roles + full permission matrix (prisma/seed.ts, docs/permission-matrix.md); no CRUD UI/API yet."
    },
    {
      "id": "adm-permissions",
      "feature": "Permissions",
      "status": "in_progress",
      "notes": "Placeholder content, gated behind requirePermission(\"permissions:manage\"). Server-side authorization is now implemented and enforced on every admin route (CLAUDE.md rule 5) \u2014 see the `auth` section above for the test suite. No CRUD UI/API for editing the matrix yet; it's defined in code (src/lib/auth/permissions.ts)."
    },
    {
      "id": "adm-college-profile",
      "feature": "College Profile",
      "status": "passing",
      "notes": "Full CMS module: singleton view/create/edit + draft/review/publish/archive workflow (src/app/admin/college-profile/). Gated on content_general:*. Covered by tests/unit/admin/college-profile/actions.test.ts."
    },
    {
      "id": "adm-departments",
      "feature": "Departments",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/departments/). Gated on content_general:*. Covered by tests/unit/admin/departments/actions.test.ts + tests/e2e/cms-departments.spec.ts (full cross-role lifecycle e2e)."
    },
    {
      "id": "adm-notices",
      "feature": "Notices",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/notices/), including publishDate/expiryDate fields. Gated on content_general:*. Covered by tests/unit/admin/notices/actions.test.ts + tests/e2e/cms-notices.spec.ts (full cross-role lifecycle e2e, 8 tests, incl. the update-requested reason banner)."
    },
    {
      "id": "adm-events",
      "feature": "Events",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/events/), including required startDate and optional endDate/location. Gated on content_general:*. Covered by tests/unit/admin/events/actions.test.ts."
    },
    {
      "id": "adm-seminars",
      "feature": "Seminars",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/seminars/), with an optional Department FK (college-wide when omitted) and speaker field. Not a distinct top-level circular section (folded into the public site's consolidated Events page alongside Workshops/Activities/Clubs \u2014 see pub-events), but given its own dedicated admin CMS module. Gated on content_general:*. Covered by tests/unit/admin/seminars/actions.test.ts."
    },
    {
      "id": "adm-workshops",
      "feature": "Workshops",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/workshops/), near-identical to Seminars but with a facilitator field instead of speaker. Gated on content_general:*. Covered by tests/unit/admin/workshops/actions.test.ts."
    },
    {
      "id": "adm-faculty",
      "feature": "Faculty",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/faculty/), including department FK select and subjectsTaught array field. Gated on content_faculty:*. Covered by tests/unit/admin/faculty/actions.test.ts + tests/e2e/cms-faculty.spec.ts."
    },
    {
      "id": "adm-staff",
      "feature": "Staff",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/staff/). Gated on content_faculty:* (shares Faculty's domain). Covered by tests/unit/admin/staff/actions.test.ts."
    },
    {
      "id": "adm-activities",
      "feature": "Activities",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/activities/), no FK. Gated on content_general:*. Covered by tests/unit/admin/activities/actions.test.ts."
    },
    {
      "id": "adm-clubs",
      "feature": "Clubs",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/clubs/), with an optional Faculty FK as advisor. Gated on content_faculty:* (the third module in this domain, alongside Faculty/Staff). Covered by tests/unit/admin/clubs/actions.test.ts."
    },
    {
      "id": "adm-infrastructure",
      "feature": "Infrastructure",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/infrastructure/), with a required InfrastructureCategory enum select. Gated on content_general:*. Covered by tests/unit/admin/infrastructure/actions.test.ts."
    },
    {
      "id": "adm-programs",
      "feature": "Programs",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/programs/), including department FK select and level enum. Gated on content_general:*. Covered by tests/unit/admin/programs/actions.test.ts."
    },
    {
      "id": "adm-admissions",
      "feature": "Admissions",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/admissions/), with a required Program FK. Gated on content_admissions:* (the first module built on this domain). Covered by tests/unit/admin/admissions/actions.test.ts + tests/e2e/cms-admissions.spec.ts (full cross-role lifecycle e2e, 6 tests)."
    },
    {
      "id": "adm-fee-structures",
      "feature": "Fee Structures",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/fee-structures/), with a required Program FK, an optional Admission FK, and a Decimal amount field. Not its own top-level circular section \u2014 folds into the public Admissions page's fee-structure table (see pub-admissions) \u2014 but given its own dedicated admin CMS module, same as Seminars/Workshops under Events. Gated on content_admissions:*. Covered by tests/unit/admin/fee-structures/actions.test.ts."
    },
    {
      "id": "adm-enrollment-statistics",
      "feature": "Enrollment Statistics",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/enrollment-statistics/), with a required Program FK and required/optional integer count fields. Folds into the public Admissions page's enrollment-statistics table (see pub-admissions). Gated on content_admissions:*. Covered by tests/unit/admin/enrollment-statistics/actions.test.ts."
    },
    {
      "id": "adm-timetables",
      "feature": "Timetables",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/timetables/), with a required Program FK and an optional JSON structuredSchedule field (rejects malformed JSON on both create and edit). Gated on content_general:*. Covered by tests/unit/admin/timetables/actions.test.ts + tests/e2e/cms-timetables.spec.ts (full lifecycle e2e, 6 tests)."
    },
    {
      "id": "adm-academic-calendar",
      "feature": "Academic Calendar",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/academic-calendar/), including required startDate/optional endDate and plain category/academicYear fields. Gated on content_general:*. Covered by tests/unit/admin/academic-calendar/actions.test.ts."
    },
    {
      "id": "adm-exams",
      "feature": "Exams",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/exams/), with a required Program FK and an optional Notice FK. Gated on content_examinations:* (the first module built on this domain). Covered by tests/unit/admin/exams/actions.test.ts."
    },
    {
      "id": "adm-results",
      "feature": "Results",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/results/), with required Program + Examination FKs and an isPublic checkbox implementing the double publish gate (see pub-results). Gated on content_examinations:*. Covered by tests/unit/admin/results/actions.test.ts + tests/e2e/cms-results.spec.ts (full cross-role lifecycle + double-gate e2e, 7 tests)."
    },
    {
      "id": "adm-documents",
      "feature": "Documents",
      "status": "passing",
      "notes": "Full document management module (src/app/admin/documents/): real file upload (replacing the former paste-a-URL flow) with server-side type/size validation (src/lib/security/upload-storage.ts \u2014 PDF/DOC(X)/XLS(X)/PPT(X)/TXT/JPEG/PNG, 25MB), client-side pre-upload preview (src/components/admin/FilePreviewInput.tsx), title/description/category metadata, optional publishDate/expiryDate scheduling window (mirrors Notice's pattern \u2014 src/lib/content.ts's isDocumentPubliclyVisible, reused identically by the file route so the two can never drift), file replacement on edit (bumps `version`, logs FILE_REPLACED), and approver tracking (Document.approvedById/approvedAt, set on the workflow's approve transition, cleared on reject/return-to-draft). Uploaded files live outside public/ and are served only through src/app/api/files/documents/[id]/route.ts, which is public exactly when a document is currently live (status PUBLISHED and within its publish/expiry window) and otherwise requires content_general:view \u2014 CLAUDE.md rule 5's 'no arbitrary file exposure' applies to guessed/bookmarked download links too, not just admin UI navigation. Gated on content_general:*. Covered by tests/unit/admin/documents/actions.test.ts, tests/unit/security/upload-storage.test.ts, tests/unit/content/queries.test.ts (isDocumentPubliclyVisible), and tests/e2e/cms-documents.spec.ts (12 tests: upload/validation/preview/metadata/replacement/publish-date scheduling/approver/unpublish/archive/unarchive/permission-gated file access)."
    },
    {
      "id": "adm-gallery",
      "feature": "Gallery",
      "status": "passing",
      "notes": "Full media management module (src/app/admin/gallery/): real image upload (replacing the former paste-a-URL flow) with server-side type/size validation (src/lib/security/upload-storage.ts \u2014 JPEG/PNG/WEBP/GIF, 10MB) and client-side preview, nested album -> item resource where adding an item uploads a fresh Media asset (image, alt text, caption, category, date) in the same Server Action, image replacement on edit (logs FILE_REPLACED). Media itself carries no status \u2014 visibility is governed by whether any wrapping GalleryItem is PUBLISHED, and images are served only through src/app/api/files/media/[id]/route.ts (public exactly then, otherwise requires content_general:view), never a public URL. Gated on content_general:*. Covered by tests/unit/admin/gallery/actions.test.ts, tests/unit/security/upload-storage.test.ts, and tests/e2e/cms-gallery.spec.ts (14 tests: upload/validation/preview/metadata/replacement/publish/unpublish/archive/permission-gated image access, plus the original nested-resource lifecycle)."
    },
    {
      "id": "adm-scholarships",
      "feature": "Scholarships",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/scholarships/), no FK. Gated on content_general:*. Covered by tests/unit/admin/scholarships/actions.test.ts."
    },
    {
      "id": "adm-student-support",
      "feature": "Student Support",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/student-support/), no FK. Gated on content_general:*. Covered by tests/unit/admin/student-support/actions.test.ts."
    },
    {
      "id": "adm-policies",
      "feature": "Policies",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/policies/), no FK. Gated on content_general:*. Covered by tests/unit/admin/policies/actions.test.ts."
    },
    {
      "id": "adm-regulations",
      "feature": "Regulations",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/regulations/), no FK. Gated on content_general:*. Covered by tests/unit/admin/regulations/actions.test.ts."
    },
    {
      "id": "adm-affiliation",
      "feature": "Affiliation",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/affiliation/), with an optional Program FK (college-wide when omitted). Gated on content_general:*. Covered by tests/unit/admin/affiliation/actions.test.ts."
    },
    {
      "id": "adm-contact",
      "feature": "Contact",
      "status": "passing",
      "notes": "Full CMS module: list/view/create/edit + draft/review/publish/archive workflow (src/app/admin/contact/), with a required ContactType enum select. Required adding publishedAt/publishedBy to Contact for the first time (prisma/migrations/20260914093028_add_workflow_fields_contact_location). Gated on content_general:*. Covered by tests/unit/admin/contact/actions.test.ts."
    },
    {
      "id": "adm-location",
      "feature": "Location",
      "status": "passing",
      "notes": "Full CMS module: singleton view/create/edit + draft/review/publish/archive workflow (src/app/admin/location/), the second true singleton after College Profile \u2014 uses findFirst rather than findUnique since Location.collegeId has no @unique constraint. Also required adding publishedAt/publishedBy to Location for the first time (same migration as Contact). Gated on content_general:*. Covered by tests/unit/admin/location/actions.test.ts + tests/e2e/cms-location.spec.ts (singleton-guard + editing-doesn't-revert-status e2e, 4 tests)."
    },
    {
      "id": "adm-grievances",
      "feature": "Grievances",
      "status": "passing",
      "notes": "Full case-management module (src/app/admin/grievances/) \u2014 its own state machine (src/lib/grievance-workflow.ts: NEW/ASSIGNED/UNDER_REVIEW/ACTION_REQUIRED/RESOLVED/CLOSED, with reopen), deliberately not the content-workflow or compliance-workflow engines since a grievance must never become public. List page has search (reference #, subject, name, description) + status/category filters + pagination (src/lib/pagination.ts, src/components/ui/Pagination.tsx). Detail page: decrypts submitterEmail/submitterPhone only for grievances:view holders, lists attachments (downloadable only via the permission-checked src/app/api/admin/grievances/[id]/attachments/[attachmentId]/route.ts, never a public URL), assignment (restricted to users holding grievances:manage \u2014 src/lib/admin/grievance-assignees.ts), internal notes, recorded responses to the submitter (no outbound email integration exists \u2014 a response is the record of what staff said, not proof of delivery), and full audit history (AuditLog rows for every create/assign/status-change/note/response). Every action requires grievances:manage; grievances:view alone can see but not act. Covered by tests/unit/grievance/workflow.test.ts, tests/unit/admin/grievances/actions.test.ts, tests/unit/admin/grievance-assignees.test.ts, tests/unit/pagination.test.ts, and tests/e2e/grievance.spec.ts (17 tests: full lifecycle across every status, access control, decrypted-contact visibility, attachment auth, search/filter)."
    },
    {
      "id": "adm-compliance",
      "feature": "Compliance Dashboard (/admin/compliance, /admin/compliance/[id]): all 20 circular requirements, each with an EXPLICIT rule (required records, required fields, required documents where appropriate, public route, responsible role, responsible module) and a completeness calculation that inspects real structured data field-by-field \u2014 never a generic \"page/record exists\" check \u2014 plus status/completeness/last updated/last verified/verifier/evidence/reviewer-notes and a verify/reject-review/evidence/verification-history workflow gated so VERIFIED can only ever be reached by an authorized human reviewer.",
      "status": "passing",
      "type": "unit+e2e",
      "notes": "ComplianceStatus migrated (prisma/migrations/20260914160000_compliance_workflow, applied with zero drift) from the old not_started/in_progress/submitted_for_review/verified/rejected set to the requested NOT_STARTED/IN_PROGRESS/READY_FOR_REVIEW/VERIFIED/NEEDS_UPDATE/NOT_APPLICABLE; VerificationDecision REJECTED renamed NEEDS_UPDATE; ComplianceVerification.rejectionReason generalized to `note`. src/lib/compliance-workflow.ts is the transition engine: submit_for_review/verify/request_update/mark_not_applicable/reopen are explicit human actions requiring the right permission, and writing a ComplianceVerification + audit log row; NOT_STARTED<->IN_PROGRESS is the one automatic transition (`syncAutomaticStatus`), and a requirement can never reach VERIFIED any way other than the `verify` action (CLAUDE.md rule 7). **src/lib/compliance.ts's `COMPLIANCE_RULES` is a fully explicit `ComplianceRule` record for all 20 items** \u2014 each declares `requiredRecords`/`requiredFields`/`requiredDocuments`/`publicRoute`/`responsibleRole`/`responsibleModule` as data, alongside a `check()` that queries the real content tables and, for nearly every item, verifies specific fields are populated on EVERY matching published record (an `everyRecordHasFields` helper), not merely that a row count is non-zero \u2014 e.g. Faculty requires every published faculty member to have a designation/qualifications/subjectsTaught/contact info, and every published Department to have at least one; Admissions requires eligibilityCriteria + both application dates on every admission cycle, plus a published Document attached as `ComplianceEvidence` (an admission notice/prospectus) via the new `hasPublishedDocumentEvidence` helper (also used for items 10, 13, 18's required documents \u2014 the actual mechanism `ComplianceEvidence`'s generic (entityType, entityId) pointer was built for, not a `Document.category` keyword guess); Contact requires a labeled published phone AND email; Grievance requires a real PRINCIPAL/ADMINISTRATOR/SUPER_ADMIN role assignment (a working, staffed mechanism), not any content table. Confirmed live against the real dev database that this genuinely changes results, not just refactors: the seeded Faculty record \u2014 previously human-VERIFIED at what the old count-only check reported as 100% \u2014 now correctly shows 67% completeness (missing qualifications and email/phone) while its VERIFIED status correctly stays untouched (automatic sync never overrides a human decision). tests/unit/compliance.test.ts rewritten (68 tests): one describe block per item 1-20, each proving the check is NOT a page-exists rubber stamp (a record existing with a missing required field is asserted as still non-100%) plus a fully-compliant case, a metadata-completeness test for all 20 rules, and the overview/detail loaders' rule-attachment + automatic-sync wiring. tests/unit/compliance-workflow.test.ts (34 tests, unchanged) + tests/unit/admin/compliance/actions.test.ts (10 tests, unchanged) + tests/e2e/cms-compliance.spec.ts (6 tests, updated for the renamed Required records/fields UI sections, with a new real-browser assertion that Faculty's missing-qualifications gap is visibly surfaced even on a VERIFIED item)."
    },
    {
      "id": "adm-compliance-report-export",
      "feature": "Compliance report generation + submission tracking (/admin/compliance/reports) \u2014 the circular's explicit \"compliance report + live website URL submitted to the Office of the Inspector of Colleges\" requirement (docs/requirements.md \u00a73, docs/compliance-matrix.md \u00a72).",
      "status": "passing",
      "type": "unit+e2e",
      "notes": "New `ComplianceReportExport` model (prisma/migrations/20260915191533_add_compliance_report_export) \u2014 previously sketched in docs/database-design.md \u00a79 as \"not necessarily a persisted table,\" now a real one. `generateComplianceReport` (src/app/admin/compliance/report-actions.ts, gated on compliance:verify) freezes a snapshot of every requirement's itemNumber/title/status/completeness/last-verification at generation time via the new `buildComplianceReportSnapshot`/`getComplianceReportExports` (src/lib/compliance.ts) \u2014 deliberately never requiring every requirement to already be VERIFIED, since a college may need to report partial progress toward the circular's 1-month launch deadline. `recordComplianceReportSubmission` sets submittedAt/submittedById and is one-way: a report already marked submitted cannot be re-submitted or have its date overwritten. Both actions write COMPLIANCE_REPORT_GENERATED/COMPLIANCE_REPORT_SUBMITTED audit entries (CLAUDE.md rule 8). UI at /admin/compliance/reports (linked from /admin/compliance): a generate-report form (compliance:verify only) plus a list of past reports with verified/total counts, website URL, and a per-row \"Mark as submitted\" action. Covered by tests/unit/compliance.test.ts's new buildComplianceReportSnapshot/getComplianceReportExports blocks, tests/unit/admin/compliance/report-actions.test.ts (11 tests: permission gating, invalid-URL rejection, partial-progress generation, audit content, one-way submission guard), and tests/e2e/compliance-report.spec.ts (2 tests: full generate\u2192list\u2192mark-submitted flow as ADMINISTRATOR, and EDITOR redirected to /admin/unauthorized). Note: per docs/permission-matrix.md, compliance:view and compliance:verify are granted to exactly the same roles (SUPER_ADMIN/PRINCIPAL/ADMINISTRATOR), so the page's \"list visible, generate form hidden\" branch (view without verify) has no seeded dev account to exercise it end-to-end \u2014 flagged, not silently untested."
    },
    {
      "id": "adm-audit-logs",
      "feature": "Audit logs",
      "status": "passing",
      "notes": "Centralized audit logging is now real and complete. Single writer: src/lib/audit.ts's logAudit() (actorId: string | null, so system/anonymous actions \u2014 public grievance submission, automatic compliance sync, failed logins against nonexistent accounts \u2014 go through the same path as authenticated ones). All 7 former direct prisma.auditLog.create() call sites now funnel through it: content-workflow.ts, compliance-workflow.ts, grievance-workflow.ts, admin/grievances/actions.ts (notes/responses), (public)/grievance/actions.ts (CREATE), auth/actions.ts (LOGIN/LOGIN_FAILED/LOGOUT, now also recording userAgent in metadata), and admin/users/actions.ts (ROLE_CHANGE, new). Read-only viewer at /admin/audit-logs (list: entityType/action/actor filters + pagination) and /admin/audit-logs/[id] (before/after snapshots + metadata as formatted JSON), gated behind requirePermission(\"audit_logs:view\"); no edit/delete UI or Server Action exists anywhere for this model. Immutability is enforced beyond the application layer too: migration 20260916000000_audit_log_hardening adds Postgres triggers that reject any UPDATE/DELETE on audit_logs outright (manually verified via psql that both throw). The admin dashboard's \"recent audit activity\" table now delegates to the same getAuditLogPage() query the full viewer uses, so the two can never disagree. Covered by tests/unit/audit.test.ts (7 cases, including the actorId: null path and metadata handling), tests/unit/admin/audit-logs.test.ts (filtering, pagination, actor fallback, detail mapping), and tests/e2e/audit-logs.spec.ts (14 tests: access control, read-only guarantees on both list and detail pages, and real CREATE/PUBLISH/GRIEVANCE_STATUS_CHANGE/LOGIN/ROLE_CHANGE entries produced by actually performing those actions end to end)."
    },
    {
      "id": "adm-approval-workflow",
      "feature": "Approval workflow",
      "status": "not_started",
      "notes": "Placeholder route exists; no ApprovalRequest table/state machine yet. Must verify no content or compliance status can be auto-approved (CLAUDE.md rule 7)."
    },
    {
      "id": "adm-content-review",
      "feature": "Content review/freshness tracking",
      "status": "passing",
      "notes": "Real per-record review tracking (src/lib/content-review.ts) for the 5 modules the task explicitly named warnings for \u2014 Notices, Faculty, Academic Calendar, Timetables, Admissions (Documents already has its own, more precise freshness signal, expiryDate, so it's out of scope here; the engine itself is generic and extending to more modules later is additive, not a redesign). Each record now has lastReviewedAt/lastReviewedById (prisma/migrations/20260917000000_content_review_tracking); every module's [id]/page.tsx shows a ReviewPanel (last updated, last reviewed, next review date, reviewer) plus a Mark reviewed action gated on the module's :publish permission (a higher-trust accuracy check, same tier as approve/publish) that writes a new MARK_REVIEWED audit entry via the centralized logAudit(). \"Next review due\" is always computed live as (lastReviewedAt ?? publishedAt ?? createdAt) + the module's configured period \u2014 never stored \u2014 so changing a period retroactively affects every record's overdue status immediately, no backfill needed. The review period is configurable per module at /admin/content-review-settings (new ReviewPeriodSetting table, one row per explicit override; DEFAULT_REVIEW_PERIOD_DAYS=180 for any module without one), gated behind compliance:verify (PRINCIPAL/ADMINISTRATOR/SUPER_ADMIN \u2014 reused rather than adding a new permission for one settings screen). The dashboard's new review-warnings section is covered under adm-dashboard above. Covered by tests/unit/content-review.test.ts (freshness math, mark-reviewed write+audit, reviewer-name resolution), tests/unit/admin/review-settings.test.ts (period get/set/validation, override detail), a markXReviewed test added to each of the 5 modules' existing actions.test.ts files, tests/unit/admin/content-review-settings/actions.test.ts, and tests/unit/admin/dashboard.test.ts's getReviewWarnings block (overdue detection, period overrides, reviewer resolution, count-vs-capped-list accuracy) \u2014 625/625 unit tests passing project-wide. New tests/e2e/content-review.spec.ts (9 tests: the full create\u2192submit\u2192publish\u2192mark-reviewed lifecycle with per-role button visibility, a MARK_REVIEWED audit entry, settings-screen access control and period updates including server-side rejection of an invalid period, and the dashboard section rendering). Also found and fixed a real pre-existing bug while running the full e2e suite: getNotices()/getImportantAnnouncement() in src/lib/content.ts ordered by `publishDate desc` with no explicit NULLS handling \u2014 Postgres's default for DESC is NULLS FIRST, so any notice with no publishDate set always outranked every dated notice as \"most current,\" regardless of actual recency. Fixed with explicit `nulls: \"last\"` on both queries."
    }
  ]
}
